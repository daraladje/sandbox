import Head from 'next/Head';

const Meta = ({ title, keywords, description }) => {
  return (
    <Head>
      <title>WebDev Newz</title>
      <meta name="keywords" conent="web dev, programming" />
    </Head>
  );
};

export default Meta;
